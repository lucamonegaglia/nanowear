#include "pedometer.h"

// Pedometer is header-only by design (see pedometer.h). This translation unit
// exists so the native test environment can compile the class as a regular
// source file (mirroring how the board build links it) and keeps behaviour
// identical across environments.
